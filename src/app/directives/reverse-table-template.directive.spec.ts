import { ReverseTableTemplateDirective } from './reverse-table-template.directive';

describe('ReverseTableTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ReverseTableTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
